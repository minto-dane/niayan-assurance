Public deterministic test fixtures only. NEVER provision these public keys as an authority.
Generated by an independent Python reference encoder, not by Ada. All private seeds are public-test derivations; no secret is shipped.
Scope, boot, times and receipts are synthetic. Signatures attest only to fixture bytes.
